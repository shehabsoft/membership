apply plugin: "war"

bootWar {
    mainClassName = "org.emu.membership.MembershipApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
